apiVersion: v1
kind: Service
metadata:
  name: green-ai-service
spec:
  selector:
    app: green-ai
  ports:
    - name: http
      port: 80
      targetPort: 8000
    - name: metrics
      port: 8001
      targetPort: 8001
  type: ClusterIP
