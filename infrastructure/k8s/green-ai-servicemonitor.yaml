apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: green-ai-servicemonitor
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: green-ai
  namespaceSelector:
    matchNames:
      - default
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
